# Environment variables for ADK Agent Examples

# --- General API Keys (Uncomment and set the ones you need) ---

# For agents using Google Gemini models directly (e.g., weather_agent_gemini)
# GOOGLE_API_KEY="YOUR_GOOGLE_API_KEY"

# For agents using OpenAI models (e.g., weather_agent_openai via LiteLLM)
# OPENAI_API_KEY="YOUR_OPENAI_API_KEY"

# For agents using Anthropic models (e.g., weather_agent_anthropic via LiteLLM)
# ANTHROPIC_API_KEY="YOUR_ANTHROPIC_API_KEY"

# --- MCP Agent Specific ---

# For mcp_agent_google_maps
# GOOGLE_MAPS_API_KEY="YOUR_GOOGLE_MAPS_API_KEY"

# --- Configuration for using Vertex AI with Gemini (Optional) ---
# If you want to use Vertex AI as the backend for Gemini models,
# uncomment and set these. Otherwise, leave them commented out if using GOOGLE_API_KEY directly.
# Ensure you have authenticated via `gcloud auth application-default login`
# and your project has the Vertex AI API enabled.
#
# GOOGLE_CLOUD_PROJECT="your-gcp-project-id"
# GOOGLE_CLOUD_LOCATION="your-gcp-region" # e.g., us-central1
# GOOGLE_GENAI_USE_VERTEXAI="True"

# --- Filesystem MCP Agent (Configuration in code) ---
# For `mcp_agent_filesystem`, the target folder path is configured directly
# in `mcp_agent_filesystem/agent.py` (the `TARGET_FOLDER_PATH` variable).
# Remember to set it to an ABSOLUTE path on your system.
# Example: TARGET_FOLDER_PATH = "/Users/yourusername/Documents/adk_filesystem_agent_data" 